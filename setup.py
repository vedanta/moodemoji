# setup.py

from setuptools import setup, find_packages

setup(
    name="moodemoji",
    version="0.1.0",
    packages=find_packages(),
    description="Map moods to emojis 🧠➡️😊",
    author="Your Name",
    install_requires=[],
)
